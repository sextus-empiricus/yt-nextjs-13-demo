import React, { ReactElement } from 'react';

interface Props {

}

const NotFoundPage = (props: Props): ReactElement => {
   return (
      <div> Custom Not Found Page | 404 </div>
   );
};

export default NotFoundPage;